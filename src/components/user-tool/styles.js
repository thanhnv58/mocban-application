const styles = theme => ({
  searchInput: {
    marginBottom: theme.spacing(2)
  },
  textField: {
    width: "100%"
  },
  button: {
    marginBottom: theme.spacing(2)
  }
});

export default styles;
